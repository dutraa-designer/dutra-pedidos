"use client";
import { FormEvent,useState } from "react";
export default function InternalLogin(){const [error,setError]=useState("");const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const password=new FormData(e.currentTarget).get("password");const r=await fetch("/api/internal-login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});if(r.ok)location.href="/controle-interno";else setError("Senha financeira inválida.")};return <main className="login-screen"><form className="login-card" onSubmit={submit}><p className="login-card-kicker">DUTRAA DESIGNER · PRIVADO</p><h2>Controle financeiro</h2><p className="login-subtitle">Digite sua senha exclusiva para continuar.</p><label>Senha<input name="password" type="password" autoComplete="off" required/></label><button type="submit">ACESSAR <span>→</span></button>{error&&<p className="notice">{error}</p>}</form></main>}

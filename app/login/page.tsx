"use client";
import { FormEvent, useState } from "react";
export default function Login(){const [error,setError]=useState(""); const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/login",{method:"POST",body:JSON.stringify({username:f.get("username"),password:f.get("password")}),headers:{"content-type":"application/json"}});if(r.ok) location.href="/"; else setError("Usuário ou senha inválidos.")};return <main className="app-shell"><form className="login-card" onSubmit={submit}><p className="eyebrow">DUTRAA DESIGNER</p><h1>Acesso ao painel</h1><input name="username" placeholder="Usuário" autoComplete="username" required/><input name="password" type="password" placeholder="Senha" autoComplete="current-password" required/><button type="submit">ENTRAR</button>{error&&<p className="notice">{error}</p>}</form></main>}

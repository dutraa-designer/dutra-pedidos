"use client";
import { FormEvent, useState } from "react";
export default function Login(){const [error,setError]=useState(""); const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/login",{method:"POST",body:JSON.stringify({username:f.get("username"),password:f.get("password")}),headers:{"content-type":"application/json"}});if(r.ok) location.href="/"; else setError("Usuário ou senha inválidos.")};return <main className="login-screen"><div className="login-glow login-glow-one"/><div className="login-glow login-glow-two"/><section className="login-layout"><div className="login-intro"><div className="login-mark">D</div><p className="eyebrow login-eyebrow">DUTRAA DESIGNER</p><h1>Seu ateliê,<br/><em>em controle.</em></h1><p>Organize cada pedido, acompanhe a produção e mantenha seus custos sempre à vista.</p><span className="login-line"/></div><form className="login-card" onSubmit={submit}><p className="login-card-kicker">ÁREA RESTRITA</p><h2>Acesso ao painel</h2><p className="login-subtitle">Entre para acompanhar seus pedidos.</p><label>Usuário<input name="username" placeholder="Digite seu usuário" autoComplete="username" required/></label><label>Senha<input name="password" type="password" placeholder="Digite sua senha" autoComplete="current-password" required/></label><button type="submit">ENTRAR <span>→</span></button>{error&&<p className="notice">{error}</p>}<small className="login-foot">DUTRAA DESIGNER · CONTROLE DE PRODUÇÃO</small></form></section></main>}
